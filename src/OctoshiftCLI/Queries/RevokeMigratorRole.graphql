mutation revokeMigratorRole(
    $organizationId: ID!
    $actor: String!
    $actor_type: ActorType!
) {
    revokeMigratorRole(
        input: {
            organizationId: $organizationId
            actor: $actor
            actorType: $actor_type
        }
    ) {
        success
    }
}